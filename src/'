import React from 'react';
import './App.css';

import { tweets } from '../../constants';

console.log(tweets);

const App = () => (
	<div>
		<h1>{tweets[1].username}</h1>
		<p><br>{tweets[1].date}</p>
		<img src={tweets[1].avatar} />
		<p>{tweets[1].fullname}</p>
		<p>{tweets[1].content}</p>
	</div>
);

export default App;
